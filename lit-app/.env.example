EXPO_PUBLIC_SUPABASE_URL=https://qepkwlchduhnqzeegpeh.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Beta signup: in Supabase Dashboard → Authentication → Providers → Email,
# turn OFF "Confirm email" so sign-up does not send confirmation emails
# (avoids the built-in SMTP rate limit for small betas).

# Evie AI Path Pipeline (api/agents/evie-path-pipeline.ts) — SERVER-ONLY.
# Set this in Vercel's project env vars, never in a client-exposed EXPO_PUBLIC_* var,
# and never commit a real key here. Without it, the route returns a deterministic
# safe_fallback plan instead of crashing.
OPENAI_API_KEY=
# Optional — defaults to gpt-4o-mini if unset.
# OPENAI_MODEL=
